[package]
name = "qualia-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "BeliefSlot, LayerSlot, LedgerEntry, HealthReport — zero deps, #[repr(C)]"

[dependencies]
